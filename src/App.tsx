import { ThemeProvider } from '@fluentui/react'

function App() {

  return (
    <ThemeProvider style={{ height: "inherit" }}>
      <h1>Welcome to React in UDO</h1>
    </ThemeProvider>
  )
}

export default App
